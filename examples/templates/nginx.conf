events { worker_connections 1024; }
http {
    server {
        listen 80;
        location / {
            return 200 'Hello from Racket makejail!\n';
            add_header Content-Type text/plain;
        }
    }
}
